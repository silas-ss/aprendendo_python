#!/usr/bin/python
#-*- coding:latin-1 -*-

distancia = float(input('Distância a percorrer: '))
velocidade_media = float(input('Velocidade média: '))

print ('Tempo de viagem: %.3f' %(distancia / velocidade_media))
